# Пошаговая инструкция: Деплой на GitHub Pages

## Шаг 1: Раскомментировать `output: 'export'` в next.config.ts

### 1.1 Откройте файл
1. Откройте файл `finassistant-ai/next.config.ts` в вашем редакторе

### 1.2 Найдите строку
Найдите эту строку (она должна быть закомментирована):
```typescript
// output: 'export',
```

### 1.3 Раскомментируйте
Измените на:
```typescript
output: 'export',
```

### 1.4 Полный файл должен выглядеть так:
```typescript
import type { NextConfig } from "next";
import path from "path";

const nextConfig: NextConfig = {
  // Enable static export for GitHub Pages
  output: 'export',  // ← Эта строка должна быть раскомментирована
  
  // Fix workspace root warning
  outputFileTracingRoot: path.join(__dirname),
  
  // For static export, images need to be unoptimized
  images: {
    unoptimized: true,
  },
  
  // Ensure environment variables are loaded
  env: {
    NEXT_PUBLIC_SUPABASE_URL: process.env.NEXT_PUBLIC_SUPABASE_URL,
    NEXT_PUBLIC_SUPABASE_ANON_KEY: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
  },
};

export default nextConfig;
```

### 1.5 Сохраните файл
Сохраните изменения (Ctrl+S или Cmd+S)

---

## Шаг 2: Добавить GitHub Secrets

### 2.1 Откройте GitHub репозиторий
1. Перейдите на GitHub.com
2. Войдите в свой аккаунт
3. Найдите репозиторий `finassistant-ai` (или ваш репозиторий)
4. Откройте его

### 2.2 Перейдите в Settings
1. В верхней части репозитория найдите вкладку **"Settings"**
2. Нажмите на неё
3. В левом меню найдите раздел **"Secrets and variables"**
4. Нажмите на **"Actions"** (под "Secrets and variables")

### 2.3 Добавьте первый Secret: NEXT_PUBLIC_SUPABASE_URL
1. Нажмите кнопку **"New repository secret"** (справа вверху)
2. В поле **"Name"** введите: `NEXT_PUBLIC_SUPABASE_URL`
3. В поле **"Secret"** вставьте ваш Supabase URL:
   - Обычно выглядит как: `https://xxxxx.supabase.co`
   - Найдите его в Supabase Dashboard → Settings → API → Project URL
4. Нажмите **"Add secret"**

### 2.4 Добавьте второй Secret: NEXT_PUBLIC_SUPABASE_ANON_KEY
1. Снова нажмите **"New repository secret"**
2. В поле **"Name"** введите: `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. В поле **"Secret"** вставьте ваш Supabase Anon Key:
   - Найдите его в Supabase Dashboard → Settings → API → Project API keys → `anon` `public`
   - Это длинный ключ, начинающийся с `eyJ...`
4. Нажмите **"Add secret"**

### 2.5 Проверьте, что оба Secrets добавлены
Вы должны увидеть в списке:
- ✅ `NEXT_PUBLIC_SUPABASE_URL`
- ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`

**Важно**: Если у вас нет доступа к Supabase:
1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Settings → API
4. Скопируйте:
   - **Project URL** → это `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** key → это `NEXT_PUBLIC_SUPABASE_ANON_KEY`

---

## Шаг 3: Включить GitHub Pages

### 3.1 Откройте настройки Pages
1. В репозитории GitHub нажмите **"Settings"** (вкладка вверху)
2. В левом меню найдите раздел **"Pages"** (внизу списка)
3. Нажмите на **"Pages"**

### 3.2 Настройте Source
1. Найдите раздел **"Source"**
2. В выпадающем списке выберите **"GitHub Actions"** (НЕ "Deploy from a branch")
3. Если вы видите предупреждение, проигнорируйте его (workflow уже создан)

### 3.3 (Опционально) Настройте Custom Domain
1. Найдите раздел **"Custom domain"**
2. В поле введите: `finassistant-ai.com`
3. Нажмите **"Save"**
4. GitHub покажет вам DNS записи (мы настроим их позже в Namecheap)

### 3.4 Сохраните настройки
Настройки сохраняются автоматически

---

## Шаг 4: Commit и Push изменения

### 4.1 Откройте терминал
Откройте PowerShell или Command Prompt в папке проекта

### 4.2 Перейдите в папку проекта
```powershell
cd C:\Users\user\Desktop\FinAssistant\finassistant-ai
```

### 4.3 Проверьте статус
```powershell
git status
```
Вы должны увидеть изменённый файл `next.config.ts`

### 4.4 Добавьте изменения
```powershell
git add next.config.ts
git add .github/workflows/deploy.yml
```

### 4.5 Создайте commit
```powershell
git commit -m "Configure for GitHub Pages deployment"
```

### 4.6 Отправьте на GitHub
```powershell
git push
```

### 4.7 Проверьте, что push прошёл успешно
Вы должны увидеть сообщение типа:
```
To https://github.com/your-username/finassistant-ai.git
   xxxxx..xxxxx  master -> master
```

---

## Шаг 5: Проверить деплой

### 5.1 Откройте вкладку Actions
1. В репозитории GitHub нажмите на вкладку **"Actions"** (вверху)
2. Вы должны увидеть запущенный workflow **"Deploy to GitHub Pages"**

### 5.2 Дождитесь завершения
1. Нажмите на запущенный workflow
2. Вы увидите процесс:
   - ✅ Checkout
   - ✅ Setup Node.js
   - ✅ Install dependencies
   - ✅ Build
   - ✅ Setup Pages
   - ✅ Upload artifact
   - ✅ Deploy to GitHub Pages

### 5.3 Проверьте результат
1. После завершения (зелёная галочка), нажмите на **"Deploy to GitHub Pages"** job
2. Внизу вы увидите ссылку на ваш сайт:
   - `https://your-username.github.io/finassistant-ai`
   - Или `https://finassistant-ai.com` (если настроили custom domain)

### 5.4 Откройте сайт
1. Скопируйте URL
2. Откройте в браузере
3. Проверьте, что landing page загружается
4. Попробуйте отправить email через форму waitlist

---

## Шаг 6: Настроить Custom Domain (Namecheap)

### 6.1 Получите DNS записи от GitHub
1. В GitHub → Settings → Pages
2. В разделе **"Custom domain"** вы увидите инструкции
3. GitHub покажет вам два варианта:

#### Вариант A: Использовать GitHub Nameservers (Проще)
GitHub покажет вам nameservers:
- `ns1.github.com`
- `ns2.github.com`

#### Вариант B: Использовать A записи (Больше контроля)
GitHub покажет вам IP адреса:
- `185.199.108.153`
- `185.199.109.153`
- `185.199.110.153`
- `185.199.111.153`

### 6.2 Настройте DNS в Namecheap

#### Если выбрали Вариант A (Nameservers):
1. Откройте Namecheap: https://www.namecheap.com
2. Войдите в аккаунт
3. Перейдите в **"Domain List"**
4. Найдите домен `finassistant-ai.com`
5. Нажмите **"Manage"** справа
6. Перейдите на вкладку **"Advanced DNS"**
7. Найдите раздел **"Nameservers"**
8. Выберите **"Custom DNS"**
9. Удалите существующие nameservers
10. Добавьте:
    - `ns1.github.com`
    - `ns2.github.com`
11. Нажмите **"Save"** (галочка)

#### Если выбрали Вариант B (A записи):
1. Откройте Namecheap → Domain List → Manage → Advanced DNS
2. В разделе **"Host Records"**:
3. Удалите все существующие A записи для `@`
4. Добавьте 4 A записи:
   - Type: `A Record`
   - Host: `@`
   - Value: `185.199.108.153`
   - TTL: `Automatic`
   - (Повторите для остальных 3 IP адресов)
5. Добавьте CNAME для www:
   - Type: `CNAME Record`
   - Host: `www`
   - Value: `your-username.github.io.` (замените your-username на ваш)
   - TTL: `Automatic`
6. Нажмите **"Save All Changes"**

### 6.3 Подождите распространения DNS
1. Обычно занимает 1-4 часа, максимум 48 часов
2. Проверьте статус: https://www.whatsmydns.net
3. Введите домен `finassistant-ai.com`
4. Проверьте, что A записи указывают на IP адреса GitHub

### 6.4 Проверьте в GitHub
1. Вернитесь в GitHub → Settings → Pages
2. В разделе **"Custom domain"** должен появиться зелёный значок ✅
3. Это означает, что DNS настроен правильно
4. GitHub автоматически выдаст SSL сертификат (займёт 10-15 минут)

### 6.5 Откройте сайт
1. Откройте `https://finassistant-ai.com` в браузере
2. Проверьте, что сайт загружается
3. Проверьте, что есть зелёный замочек (SSL работает)

---

## Troubleshooting (Решение проблем)

### Проблема: Build fails в GitHub Actions
**Решение:**
1. Откройте Actions → Failed workflow → Build job
2. Проверьте логи, найдите ошибку
3. Убедитесь, что:
   - `output: 'export'` раскомментирован в `next.config.ts`
   - GitHub Secrets добавлены правильно
   - Нет синтаксических ошибок в коде

### Проблема: Secrets не работают
**Решение:**
1. Проверьте, что Secrets названы точно:
   - `NEXT_PUBLIC_SUPABASE_URL` (с заглавными буквами)
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` (с заглавными буквами)
2. Убедитесь, что значения скопированы полностью (без пробелов)
3. Пересоздайте Secrets, если нужно

### Проблема: Домен не работает
**Решение:**
1. Подождите до 48 часов (обычно 1-4 часа)
2. Проверьте DNS: https://www.whatsmydns.net
3. Убедитесь, что DNS записи правильные в Namecheap
4. Проверьте, что домен не заблокирован
5. Очистите кэш браузера (Ctrl+Shift+Delete)

### Проблема: SSL сертификат не выдаётся
**Решение:**
1. Подождите 10-15 минут после настройки DNS
2. Убедитесь, что DNS распространился (проверьте whatsmydns.net)
3. В GitHub → Settings → Pages должно быть зелёное ✅
4. Если не помогает, обратитесь в поддержку GitHub

### Проблема: Waitlist форма не работает
**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки
3. Убедитесь, что:
   - Supabase URL и Key правильные
   - Таблица `waitlist` создана в Supabase
   - RLS политики настроены правильно

---

## Чеклист перед деплоем

- [ ] `output: 'export'` раскомментирован в `next.config.ts`
- [ ] GitHub Secret `NEXT_PUBLIC_SUPABASE_URL` добавлен
- [ ] GitHub Secret `NEXT_PUBLIC_SUPABASE_ANON_KEY` добавлен
- [ ] GitHub Pages включён (Source: GitHub Actions)
- [ ] Изменения закоммичены и запушены
- [ ] GitHub Actions workflow запустился
- [ ] Build прошёл успешно (зелёная галочка)
- [ ] Сайт открывается по GitHub Pages URL
- [ ] Waitlist форма работает
- [ ] (Опционально) Custom domain настроен в Namecheap
- [ ] (Опционально) DNS распространился
- [ ] (Опционально) SSL сертификат выдан
- [ ] (Опционально) Сайт работает на `https://finassistant-ai.com`

---

## Готово! 🎉

После выполнения всех шагов ваш сайт будет доступен:
- **GitHub Pages**: `https://your-username.github.io/finassistant-ai`
- **Custom Domain**: `https://finassistant-ai.com` (после настройки DNS)

Каждый раз, когда вы делаете `git push`, сайт автоматически обновится!

---

## Нужна помощь?

Если что-то не работает:
1. Проверьте логи в GitHub Actions
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все шаги выполнены
4. Проверьте, что нет опечаток в Secrets
