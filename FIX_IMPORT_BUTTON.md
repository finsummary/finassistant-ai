# Исправление неактивной кнопки Import CSV

## Проблема

Кнопка "Import" неактивна (disabled) и нельзя загрузить CSV файл.

## Причина

Кнопка "Import" становится активной только когда выполнены **два условия**:

1. ✅ **CSV файл выбран** (`csvFile` не пустой)
2. ✅ **Выбран account** (`importAccountId` не пустой)

Если **account не выбран** - кнопка будет неактивна!

## Решение

### Шаг 1: Создайте Account (если его нет)

Если у вас нет bank accounts, сначала создайте его:

1. **На Dashboard найдите секцию "Bank Accounts"**
2. **Нажмите "Add manual account"** (кнопка должна быть ниже списка accounts)
3. **Заполните форму:**
   - Account Name: например "Main Bank Account"
   - Currency: например "GBP" или "USD"
4. **Нажмите кнопку создания** (обычно "Add" или "Create")

После создания account автоматически выберется для импорта.

### Шаг 2: Выберите Account для импорта

Если accounts уже есть:

1. **Найдите секцию "Import CSV"**
2. **Найдите выпадающий список "Select Account"** (обычно находится под полем выбора файла)
3. **Выберите account** из списка

### Шаг 3: Выберите CSV файл

1. **Нажмите "Choose File"** или перетащите файл
2. **Выберите CSV файл** (например: `example-transactions.csv`)

### Шаг 4: Кнопка станет активной!

После выбора:
- ✅ CSV файла
- ✅ Account

**Кнопка "Import" станет активной!** 🎉

## Проверка

Убедитесь, что:

1. ✅ **Account создан:**
   - В секции "Bank Accounts" должен быть хотя бы один account

2. ✅ **Account выбран для импорта:**
   - В секции "Import CSV" должен быть выбран account в выпадающем списке

3. ✅ **CSV файл выбран:**
   - В поле "Choose File" должно быть имя файла (например: `example-transactions.csv`)

4. ✅ **Кнопка "Import" активна:**
   - Кнопка не должна быть серой/неактивной

## Порядок действий

```
1. Создать Account
   ↓
2. Выбрать Account в "Import CSV"
   ↓
3. Выбрать CSV файл
   ↓
4. Кнопка "Import" активна → Нажать "Import"
```

## Пример использования

1. **Создайте account:**
   - Account Name: "Main Bank"
   - Currency: "GBP"
   - Нажмите "Add"

2. **Выберите account:**
   - В "Import CSV" → выпадающий список → выберите "Main Bank"

3. **Выберите файл:**
   - "Choose File" → выберите `example-transactions.csv`

4. **Импортируйте:**
   - Кнопка "Import" теперь активна
   - Нажмите "Import"

## Если проблема сохраняется

Если после создания account и выбора файла кнопка все еще неактивна:

1. **Обновите страницу** (`F5`)
2. **Проверьте консоль браузера** (F12) на наличие ошибок
3. **Убедитесь, что account действительно создан** - он должен быть в списке "Bank Accounts"

## Готово!

После создания account и выбора CSV файла кнопка "Import" должна стать активной! 🚀
