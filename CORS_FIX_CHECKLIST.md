# Чеклист исправления CORS ошибки

## ✅ Проверка настроек в Supabase Dashboard

### 1. URL Configuration (КРИТИЧЕСКИ ВАЖНО!)

1. Откройте: https://supabase.com/dashboard
2. Выберите проект "FinAssistant.ai"
3. Перейдите: **Authentication → URL Configuration**

**Проверьте следующие настройки:**

#### Site URL:
```
✅ Должно быть: http://localhost:3004
❌ НЕ должно быть: https://localhost:3004
❌ НЕ должно быть: http://localhost:3004/
❌ НЕ должно быть: пустое поле
```

#### Redirect URLs:
Должны быть добавлены (каждая на отдельной строке):
```
✅ http://localhost:3004/**
✅ http://localhost:3004/auth/callback
✅ http://localhost:3004/dashboard
```

**Как добавить:**
1. Нажмите "+ Add URL" или вставьте в поле через запятую
2. Добавьте каждую строку отдельно
3. **Обязательно нажмите "Save"** внизу страницы!

### 2. Email Provider Settings

1. **Authentication → Providers → Email**

**Проверьте:**
- ✅ Email provider включен (Enabled)
- ✅ Для разработки: "Disable email confirmations" включено
  - Это позволит сразу входить после регистрации

### 3. Проверка URL проекта

**Убедитесь, что URL проекта правильный:**

В `.env.local` должно быть:
```env
NEXT_PUBLIC_SUPABASE_URL=https://zqqhzbthcqllbfnpgptpn.supabase.co
```

**Проверьте в Supabase Dashboard:**
- Settings → API → Project URL
- Должен совпадать с `.env.local`

## ✅ Проверка локальных настроек

### 1. .env.local файл

Убедитесь, что файл содержит:
```env
NEXT_PUBLIC_SUPABASE_URL=https://zqqhzbthcqllbfnpgptpn.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...
```

**Проверьте:**
- ✅ Файл находится в `finassistant-ai/.env.local`
- ✅ Нет пробелов вокруг `=`
- ✅ Нет кавычек вокруг значений
- ✅ URL заканчивается на `.supabase.co` (не на `/`)

### 2. Dev Server

**После изменения `.env.local` или настроек Supabase:**

1. **Остановите сервер:**
   - `Ctrl+C` в терминале
   - Дождитесь полной остановки

2. **Запустите снова:**
   ```bash
   cd finassistant-ai
   npm run dev
   ```

3. **Дождитесь "Ready":**
   ```
   ✓ Ready in X seconds
   ○ Local: http://localhost:3004
   ```

### 3. Браузер

1. **Очистите кеш:**
   - `Ctrl+Shift+R` (hard refresh)
   - Или `Ctrl+F5`

2. **Или откройте в инкогнито:**
   - `Ctrl+Shift+N` (Chrome/Edge)
   - Откройте `http://localhost:3004/login`

## 🔍 Диагностика

### Если ошибка сохраняется:

1. **Проверьте консоль браузера (F12):**
   - Откройте вкладку "Console"
   - Откройте вкладку "Network"
   - Попробуйте зарегистрироваться
   - Найдите запрос к `/auth/v1/signup`
   - Посмотрите заголовки ответа (Response Headers)
   - Должен быть `Access-Control-Allow-Origin: http://localhost:3004`

2. **Проверьте URL в ошибке:**
   - В консоли ошибка показывает URL запроса
   - Убедитесь, что он начинается с `https://zqqhzbthcqllbfnpgptpn.supabase.co`
   - Если другой - проблема в `.env.local`

3. **Проверьте статус проекта:**
   - В Supabase Dashboard проверьте, что проект **Active** (не Paused)
   - Если на паузе - нажмите "Resume"

## ⚠️ Частые ошибки

1. **Site URL не сохранен:**
   - Вы изменили, но забыли нажать "Save"
   - Решение: вернитесь в URL Configuration и нажмите "Save"

2. **Redirect URLs не добавлены:**
   - Поле пустое или только один URL
   - Решение: добавьте все три URL и нажмите "Save"

3. **Сервер не перезапущен:**
   - Изменили настройки, но сервер работает со старыми
   - Решение: остановите (`Ctrl+C`) и запустите снова

4. **Кеш браузера:**
   - Браузер использует старые настройки
   - Решение: `Ctrl+Shift+R` или инкогнито режим

## 📝 После исправления

После правильной настройки:

1. Ошибка CORS должна исчезнуть
2. Регистрация должна работать
3. После регистрации вы должны быть перенаправлены на `/dashboard`

## 🆘 Если все еще не работает

Сообщите:
1. Что показывает Site URL в Supabase Dashboard?
2. Какие Redirect URLs добавлены?
3. Перезапускали ли вы сервер после изменения?
4. Пробовали ли вы в инкогнито режиме?
