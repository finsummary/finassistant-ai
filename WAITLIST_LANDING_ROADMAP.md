# Roadmap: Landing Page для Waitlist

## 🎯 Цель

Создать простую landing page для сбора email адресов в waitlist с минимальными зависимостями.

## 📋 План реализации

### Этап 1: Подготовка базы данных (Supabase)

**Время:** 15 минут

1. **Создать таблицу `waitlist` в Supabase:**
   ```sql
   CREATE TABLE IF NOT EXISTS public.waitlist (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     email TEXT NOT NULL UNIQUE,
     name TEXT,
     created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
     status TEXT DEFAULT 'pending', -- pending, approved, notified
     metadata JSONB -- для дополнительной информации
   );

   -- Индекс для быстрого поиска
   CREATE INDEX IF NOT EXISTS waitlist_email_idx ON public.waitlist(email);
   CREATE INDEX IF NOT EXISTS waitlist_created_at_idx ON public.waitlist(created_at);

   -- RLS политики (опционально, если нужна публичная регистрация)
   ALTER TABLE public.waitlist ENABLE ROW LEVEL SECURITY;

   -- Политика: любой может добавлять email
   CREATE POLICY waitlist_insert ON public.waitlist
     FOR INSERT
     TO anon, authenticated
     WITH CHECK (true);

   -- Политика: только аутентифицированные могут читать (для админки)
   CREATE POLICY waitlist_select ON public.waitlist
     FOR SELECT
     TO authenticated
     USING (true);
   ```

2. **Проверить в Supabase Dashboard:**
   - Table Editor → `waitlist`
   - Убедиться, что таблица создана

### Этап 2: Создание API Endpoint (Next.js)

**Время:** 30 минут

1. **Создать API route:** `src/app/api/waitlist/route.ts`
   - POST: добавление email в waitlist
   - GET: получение списка (для админки, опционально)
   - Валидация email
   - Проверка дубликатов
   - Возврат успешного ответа

2. **Функционал:**
   - Валидация email формата
   - Проверка на дубликаты
   - Сохранение в Supabase
   - Обработка ошибок
   - Rate limiting (опционально)

### Этап 3: Создание Landing Page

**Время:** 1-2 часа

1. **Создать страницу:** `src/app/waitlist/page.tsx`
   - Простой, чистый дизайн
   - Hero секция с заголовком и описанием
   - Форма с полями: email, name (опционально)
   - Кнопка "Join Waitlist"
   - Сообщения об успехе/ошибке
   - Footer с контактами (опционально)

2. **Дизайн:**
   - Использовать существующие компоненты Shadcn UI
   - Адаптивный дизайн (mobile-first)
   - Простая анимация при отправке
   - Loading state

3. **Компоненты:**
   - `WaitlistForm` - форма с валидацией
   - `SuccessMessage` - сообщение об успехе
   - `ErrorMessage` - сообщение об ошибке

### Этап 4: Настройка GitHub Pages (опционально)

**Время:** 30 минут

**Вариант A: Использовать существующий Next.js проект**

1. **Настроить экспорт статики:**
   - Добавить `output: 'export'` в `next.config.js` (только для waitlist страницы)
   - Или использовать Next.js как есть (рекомендуется)

2. **Деплой на Vercel (рекомендуется):**
   - Подключить GitHub репозиторий
   - Автоматический деплой
   - Бесплатный хостинг
   - Custom domain (опционально)

**Вариант B: Отдельный статический сайт для GitHub Pages**

1. **Создать отдельную папку:** `waitlist-landing/`
   - Простой HTML/CSS/JS
   - Вызов API через fetch
   - Минимальные зависимости

2. **GitHub Pages:**
   - Настроить в Settings → Pages
   - Выбрать branch и folder
   - Получить URL: `https://username.github.io/repo-name/`

### Этап 5: Дополнительные функции (опционально)

**Время:** 1-2 часа

1. **Email подтверждение:**
   - Отправка подтверждающего email через Supabase Auth или Email service
   - Double opt-in для waitlist

2. **Админка для просмотра waitlist:**
   - Страница `/admin/waitlist` (требует аутентификации)
   - Таблица с email адресами
   - Экспорт в CSV
   - Статистика (количество, даты)

3. **Аналитика:**
   - Google Analytics (опционально)
   - Отслеживание конверсий

4. **A/B тестирование:**
   - Разные варианты заголовков
   - Разные CTA кнопки

## 🛠 Технический стек

### Обязательно:
- **Next.js** - уже используется в проекте
- **Supabase** - уже настроен
- **Shadcn UI** - уже используется
- **TypeScript** - уже используется

### Опционально:
- **Vercel** - для деплоя (рекомендуется)
- **GitHub Pages** - для статического сайта
- **Resend/SendGrid** - для email отправки
- **Google Analytics** - для аналитики

## 📁 Структура файлов

```
finassistant-ai/
├── src/
│   ├── app/
│   │   ├── waitlist/
│   │   │   └── page.tsx          # Landing page
│   │   └── api/
│   │       └── waitlist/
│   │           └── route.ts      # API endpoint
│   └── components/
│       └── waitlist/
│           ├── WaitlistForm.tsx
│           ├── SuccessMessage.tsx
│           └── ErrorMessage.tsx
├── supabase/
│   └── migrations/
│       └── 20250204000000_create_waitlist.sql
└── WAITLIST_LANDING_ROADMAP.md
```

## 🚀 Быстрый старт

### Шаг 1: Создать миграцию

```bash
# Создать файл миграции
touch supabase/migrations/20250204000000_create_waitlist.sql
```

### Шаг 2: Выполнить миграцию в Supabase

1. Открыть Supabase Dashboard
2. SQL Editor → New query
3. Вставить SQL из миграции
4. Run

### Шаг 3: Создать API endpoint

Создать `src/app/api/waitlist/route.ts`

### Шаг 4: Создать landing page

Создать `src/app/waitlist/page.tsx`

### Шаг 5: Протестировать локально

```bash
npm run dev
# Открыть http://localhost:3004/waitlist
```

### Шаг 6: Деплой

**Вариант A: Vercel (рекомендуется)**
```bash
# Установить Vercel CLI
npm i -g vercel

# Деплой
vercel
```

**Вариант B: GitHub Pages**
- Настроить в Settings → Pages
- Выбрать branch и folder

## 📊 Примеры дизайна

### Минималистичный вариант:
- Центрированный контент
- Большой заголовок
- Простая форма
- Минимум элементов

### Современный вариант:
- Hero секция с градиентом
- Иконки/иллюстрации
- Анимации при hover
- Social proof (опционально)

## ✅ Чеклист

- [ ] Создать таблицу `waitlist` в Supabase
- [ ] Создать API endpoint `/api/waitlist`
- [ ] Создать landing page `/waitlist`
- [ ] Добавить валидацию email
- [ ] Добавить обработку ошибок
- [ ] Протестировать локально
- [ ] Настроить деплой (Vercel/GitHub Pages)
- [ ] Протестировать на production
- [ ] Добавить аналитику (опционально)
- [ ] Настроить custom domain (опционально)

## 🎨 Рекомендации по дизайну

1. **Простота** - минимум элементов, фокус на CTA
2. **Ясность** - понятный заголовок и описание
3. **Доверие** - упомянуть, что email не будет спамиться
4. **Мобильность** - адаптивный дизайн
5. **Скорость** - быстрая загрузка страницы

## 🔒 Безопасность

1. **Валидация email** - на клиенте и сервере
2. **Rate limiting** - ограничение запросов с одного IP
3. **RLS политики** - правильные настройки в Supabase
4. **CORS** - настройка для API (если отдельный домен)

## 📈 Метрики для отслеживания

- Количество регистраций в waitlist
- Конверсия (посетители → регистрации)
- Источники трафика
- Время на странице
- Bounce rate

## 🚦 Приоритеты

### Must Have (MVP):
1. ✅ Таблица waitlist в Supabase
2. ✅ API endpoint для добавления email
3. ✅ Простая landing page с формой
4. ✅ Валидация и обработка ошибок
5. ✅ Базовый деплой

### Should Have:
1. Email подтверждение
2. Админка для просмотра списка
3. Аналитика

### Nice to Have:
1. A/B тестирование
2. Social proof
3. Custom domain
4. Email уведомления при новых регистрациях

## 💡 Следующие шаги

1. **Начать с MVP** - минимальный функционал
2. **Протестировать** - убедиться, что все работает
3. **Добавить аналитику** - отслеживать метрики
4. **Итеративно улучшать** - на основе данных

---

**Готов начать реализацию?** Скажите, с какого этапа начать!
