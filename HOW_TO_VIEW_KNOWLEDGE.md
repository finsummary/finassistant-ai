# Где хранятся знания и как их посмотреть

## 📍 Где хранятся знания

Знания хранятся в базе данных **Supabase (PostgreSQL)** в двух таблицах:

1. **`financial_knowledge`** - общие финансовые знания (для всех пользователей)
2. **`user_knowledge`** - персональные знания пользователя (только для вас)

## 🔍 Способы просмотра

### Способ 1: Supabase Dashboard (Визуальный интерфейс)

1. **Откройте Supabase Dashboard:**
   - Перейдите на: https://supabase.com/dashboard
   - Войдите в свой аккаунт
   - Выберите проект **"FinAssistant.ai"**

2. **Перейдите в Table Editor:**
   - В левом меню нажмите **"Table Editor"**
   - Найдите таблицу **`financial_knowledge`** в списке
   - Найдите таблицу **`user_knowledge`** в списке

3. **Просмотр данных:**
   - Нажмите на название таблицы
   - Вы увидите все записи с колонками:
     - `id` - уникальный идентификатор
     - `content` - текст знания
     - `category` - категория (для financial_knowledge)
     - `type` - тип (для user_knowledge)
     - `embedding` - векторное представление (не читается человеком)
     - `created_at` - дата создания

### Способ 2: SQL Editor (SQL запросы)

1. **Откройте SQL Editor:**
   - В левом меню нажмите **"SQL Editor"**
   - Нажмите **"New query"**

2. **Выполните запрос:**

   **Для financial_knowledge:**
   ```sql
   SELECT 
     id,
     content,
     category,
     tags,
     source,
     created_at
   FROM public.financial_knowledge
   ORDER BY created_at DESC;
   ```

   **Для user_knowledge:**
   ```sql
   SELECT 
     id,
     user_id,
     content,
     type,
     metadata,
     created_at
   FROM public.user_knowledge
   WHERE user_id = auth.uid()  -- только ваши знания
   ORDER BY created_at DESC;
   ```

3. **Нажмите "Run"** для выполнения запроса

### Способ 3: Через скрипт (командная строка)

```bash
cd finassistant-ai
npm run view-knowledge
```

Этот скрипт покажет:
- Все 15 элементов financial_knowledge
- Все ваши user_knowledge записи
- Краткое содержание каждого элемента

### Способ 4: В приложении (можно добавить)

Можно создать страницу в приложении для просмотра знаний через UI.

## 📊 Структура таблиц

### `financial_knowledge`
- **Назначение:** Общие финансовые знания для всех пользователей
- **Колонки:**
  - `id` (uuid) - уникальный ID
  - `content` (text) - текст знания
  - `embedding` (vector) - векторное представление для поиска
  - `category` (text) - категория: cash_flow, expenses, risk, runway, budgeting, revenue
  - `tags` (text[]) - теги для фильтрации
  - `source` (text) - источник знания
  - `created_at` (timestamp) - дата создания

### `user_knowledge`
- **Назначение:** Персональные знания пользователя
- **Колонки:**
  - `id` (uuid) - уникальный ID
  - `user_id` (uuid) - ID пользователя (связь с auth.users)
  - `content` (text) - текст знания
  - `embedding` (vector) - векторное представление
  - `type` (text) - тип: preference, decision_history, business_context, note
  - `metadata` (jsonb) - дополнительные данные
  - `created_at` (timestamp) - дата создания

## 🔐 Безопасность

- **`financial_knowledge`** - публичная таблица (все пользователи видят одинаковые знания)
- **`user_knowledge`** - защищена Row Level Security (RLS)
  - Каждый пользователь видит только свои знания
  - Нельзя увидеть знания других пользователей

## 🎯 Где используются знания

Знания автоматически используются в AI-анализе всех секций Framework:
- **STATE** - для анализа текущего состояния
- **DELTA** - для анализа изменений
- **TRAJECTORY** - для прогнозирования
- **EXPOSURE** - для оценки рисков
- **CHOICE** - для рекомендаций

AI ищет релевантные знания на основе вашего финансового контекста и использует их для более точных рекомендаций.
