# Настройка AI Categorize (опционально)

## Текущая ситуация

Ошибка "No AI key configured" означает, что AI Categorize **не настроен**.

Это **НЕ критично** - категории можно назначить **вручную**!

## Вариант 1: Назначить категории вручную (рекомендуется для начала)

**Это самый простой способ:**

1. **Откройте Dashboard**
2. **Найдите транзакции** в списке банковских счетов
3. **Для каждой транзакции:**
   - Найдите выпадающий список "Category" в строке транзакции
   - Выберите категорию (Income или Expense)
   - Изменения сохраняются автоматически

4. **После назначения категорий:**
   - Откройте Reports - данные должны появиться
   - Откройте Settings → Categories - статистика должна отображаться

## Вариант 2: Настроить AI Categorize (опционально)

Если хотите использовать AI для автоматической категоризации:

### Шаг 1: Получите API ключ

**Выберите один из вариантов:**

#### Вариант A: OpenAI (GPT-4o-mini)

1. **Откройте:** https://platform.openai.com/api-keys
2. **Создайте аккаунт** или войдите
3. **Нажмите "Create new secret key"**
4. **Скопируйте ключ** (начинается с `sk-...`)

**Стоимость:** ~$0.15 за 1M токенов (очень дешево для MVP)

#### Вариант B: Google Gemini (бесплатно)

1. **Откройте:** https://makersuite.google.com/app/apikey
2. **Войдите** в Google аккаунт
3. **Нажмите "Create API Key"**
4. **Скопируйте ключ**

**Стоимость:** Бесплатно для начала

### Шаг 2: Добавьте ключ в .env.local

Откройте файл `finassistant-ai/.env.local` и добавьте:

**Для OpenAI:**
```env
OPENAI_API_KEY=sk-your-key-here
AI_PROVIDER=openai
```

**Для Gemini:**
```env
GEMINI_API_KEY=your-key-here
AI_PROVIDER=gemini
```

### Шаг 3: Перезапустите dev server

**КРИТИЧЕСКИ ВАЖНО** после изменения `.env.local`:

```bash
# Остановите сервер (Ctrl+C)
cd finassistant-ai
npm run dev
```

### Шаг 4: Попробуйте AI Categorize

1. **Dashboard → "AI Categorize"**
2. **Нажмите кнопку**
3. **Дождитесь завершения** (может занять несколько секунд)
4. **Категории будут назначены автоматически**

## Что делать сейчас?

**Рекомендация:** Начните с **ручной категоризации** (Вариант 1):

1. Это быстрее для начала
2. Не требует API ключей
3. Дает контроль над категориями
4. После назначения категорий Reports и Categories заработают

**AI Categorize можно настроить позже**, если понадобится автоматизация.

## После ручной категоризации

После того, как вы назначите категории вручную:

1. ✅ **Reports** будет показывать данные по категориям
2. ✅ **Settings → Categories** будет показывать статистику
3. ✅ **Framework** будет работать с категоризированными данными

## Готово!

**Начните с ручной категоризации** - это самое простое решение для начала работы! 🚀
