# Шаг 1: Создание таблицы Waitlist в Supabase

## ✅ Что сделано

Создан файл миграции: `supabase/migrations/20250204000000_create_waitlist.sql`

## 📋 Что нужно сделать сейчас

### Вариант A: Через Supabase Dashboard (рекомендуется)

1. **Откройте Supabase Dashboard:**
   - Перейдите на: https://supabase.com/dashboard
   - Войдите в свой аккаунт
   - Выберите проект **"FinAssistant.ai"**

2. **Откройте SQL Editor:**
   - В левом меню нажмите **"SQL Editor"**
   - Нажмите **"New query"**

3. **Скопируйте и выполните SQL:**
   - Откройте файл: `supabase/migrations/20250204000000_create_waitlist.sql`
   - Скопируйте весь SQL код
   - Вставьте в SQL Editor
   - Нажмите **"Run"** (или Ctrl+Enter)

4. **Проверьте результат:**
   - Должно появиться сообщение "Success. No rows returned"
   - Перейдите в **Table Editor**
   - Найдите таблицу **`waitlist`**
   - Убедитесь, что таблица создана

### Вариант B: Через Supabase CLI (если установлен)

```bash
cd finassistant-ai
supabase db push
```

## 🔍 Проверка

После выполнения миграции проверьте:

1. **Таблица создана:**
   - Table Editor → `waitlist`
   - Должны быть колонки: `id`, `email`, `name`, `created_at`, `status`, `metadata`, `updated_at`

2. **Индексы созданы:**
   - В Table Editor нажмите на таблицу `waitlist`
   - Проверьте, что есть индексы на `email`, `created_at`, `status`

3. **RLS политики:**
   - Settings → Authentication → Policies
   - Должны быть политики: `waitlist_insert`, `waitlist_select`, `waitlist_update`

## 📊 Структура таблицы

```sql
waitlist
├── id (UUID, Primary Key)
├── email (TEXT, Unique, Not Null)
├── name (TEXT, Optional)
├── created_at (TIMESTAMPTZ, Default: NOW())
├── status (TEXT, Default: 'pending')
├── metadata (JSONB, Optional)
└── updated_at (TIMESTAMPTZ, Auto-updated)
```

## ✅ Готово к следующему шагу

Когда таблица создана и проверена, переходим к **Шагу 2: Создание API Endpoint**.

---

**Скажите, когда выполните миграцию, и я создам API endpoint!** 🚀
