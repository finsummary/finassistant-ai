# Уточнение по настройке

## Про организацию

Если вы уже создали организацию, но видите сообщение "Please set up your organization", это означает одно из:

1. **Миграции не выполнены** (вероятнее всего)
   - Если таблица `Organizations` не существует в базе данных, приложение не может загрузить организацию
   - Решение: выполните миграции (см. ниже)

2. **Проблема с загрузкой данных**
   - Обновите страницу Dashboard (`F5` или `Ctrl+R`)
   - Если проблема сохраняется - выполните миграции

## Про CSV файл

✅ **У вас уже есть пример CSV файла!**

Файл находится здесь:
```
finassistant-ai/public/example-transactions.csv
```

**Содержимое:**
- Заголовки: `Date,Description,Amount,Currency`
- Примеры транзакций с положительными (доходы) и отрицательными (расходы) суммами
- Формат дат: `YYYY-MM-DD` (например: `2024-01-15`)

**Как использовать:**
1. На Dashboard найдите секцию "Import CSV"
2. Нажмите "Choose File" или перетащите файл
3. Выберите `public/example-transactions.csv` (или скопируйте его)
4. Нажмите "Upload"

**Или создайте свой CSV:**
- Минимальные колонки: `Date`, `Amount`
- Опциональные: `Description`, `Currency`
- Формат дат: `YYYY-MM-DD` или `DD/MM/YYYY`
- Суммы: положительные для доходов, отрицательные для расходов (или используйте "Invert sign")

## Правильный порядок действий

### 1. Выполните миграции (СНАЧАЛА ЭТО!)

**ВАЖНО**: Миграции нужно выполнить ДО использования приложения.

1. **Откройте Supabase Dashboard:**
   - https://supabase.com/dashboard
   - Проект "FinAssistant.ai"

2. **SQL Editor:**
   - Левое меню → "SQL Editor"
   - Нажмите "New query"

3. **Выполните миграции:**
   - Откройте файл: `finassistant-ai/supabase/migrations/ALL_MIGRATIONS.sql`
   - Скопируйте **весь содержимое**
   - Вставьте в SQL Editor
   - Нажмите "Run" (или Ctrl+Enter)

4. **Проверьте:**
   - Должно быть сообщение "Success"
   - Database → Tables - должны быть созданы все таблицы

### 2. Создайте организацию (если еще не создана)

**После миграций:**

1. Откройте: `http://localhost:3004/settings/organization`
2. Если организация уже создана - она должна загрузиться автоматически
3. Если нет - заполните форму и сохраните

### 3. Обновите Dashboard

После миграций и создания организации:

1. Откройте Dashboard: `http://localhost:3004/dashboard`
2. Обновите страницу (`F5`)
3. Предупреждение об организации должно исчезнуть

### 4. Загрузите CSV

**Используйте пример файл:**

1. Найдите файл: `finassistant-ai/public/example-transactions.csv`
2. На Dashboard → "Import CSV"
3. Выберите файл `example-transactions.csv`
4. Или скопируйте его и используйте свою копию
5. Нажмите "Upload"

## Проверка после миграций

После выполнения миграций проверьте:

1. ✅ **Организация загружается:**
   - Откройте Settings → Organization
   - Должна отображаться созданная вами организация

2. ✅ **Dashboard не показывает предупреждение:**
   - Обновите Dashboard (`F5`)
   - Сообщение "Please set up your organization" должно исчезнуть

3. ✅ **Можно загружать CSV:**
   - Попробуйте загрузить `example-transactions.csv`
   - Транзакции должны появиться в списке

## Резюме

- **Организация уже создана?** → Отлично! Но сначала выполните миграции
- **CSV файл:** → Используйте `public/example-transactions.csv` (уже есть в проекте)
- **Порядок:** → Сначала миграции, потом все остальное

## Готово!

После выполнения миграций все должно работать правильно! 🎉
